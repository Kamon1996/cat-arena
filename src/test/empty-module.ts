// Test stub: vitest aliases the Next bundler markers `server-only` / `client-only`
// to this empty module so server/client-only modules can be unit-tested without
// triggering their intentional "wrong environment" throw.
export {};
